define(['jquery', 'survey', 'login', 'dispatches', 'libraries', 'table', 'variables', 'jquery.tablesorter', 'bootstrap', 'bootstrap.datepicker', 'bootstrap.tokenfield',
    'typeahead', 'underscore', 'jquery.textrange'], function ($, survey, login, dispatches, libraries, table, variables) {


    survey.init();
    login.init();
    dispatches.init();
    libraries.init();
    table.init();
    variables.init();

});